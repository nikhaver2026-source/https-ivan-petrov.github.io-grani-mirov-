pluginManagement {
    repositories { google(); mavenCentral(); gradlePluginPortal() }
}
dependencyResolutionManagement {
    repositories { google(); mavenCentral() }
}
rootProject.name = "GraniMirov"
include ':app'
/* Записи игры (sounds) — пакет ресурсов, который Google Play ставит вместе с
   приложением: основной модуль AAB не может быть больше двухсот мегабайт. */
include ':gamedata'
