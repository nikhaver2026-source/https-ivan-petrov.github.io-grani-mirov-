# Звуковая система «Грани Миров»

Документ описывает, как устроен звук игры после переработки по техническому
заданию: музыка — месту, звук — действию и событию, атмосфера — миру, объём —
физике мира, речь — сведениям для игрока. Музыки вне тем мест в игре нет:
вехи отмечают живые записи из свободных фэнтезийных игр. Всё, что здесь названо, проверяется
наборами 98–101 (`tests/`), а прежние свойства — наборами 16, 18, 31, 36, 39,
55, 56, 57, 85, 96.

## 1. Каналы

Девять независимых каналов, у каждого своя громкость в настройках; ни один не
роняет другой. Скорость и громкость речи (TTS) не трогают ни один звуковой
канал; при речи приглушаются только музыка (на долю ползунка «приглушение»),
фон (вполовину этой доли) и голоса людей (на треть), мир, бой и чары — нет.

| Канал | Ключ настройки | Что идёт |
|---|---|---|
| MASTER | `masterVol` | множитель поверх всех каналов; в ползунках — «Общая громкость» |
| MUSIC | `musicVol` | заглавная тема и, если включена, фоновая музыка мест; больше ничего |
| AMBIENCE | `ambVol` | фон места, живой фон, наборы окружения |
| SFX (мир) | `fxVol` | шаги, вещи, двери, сундуки, погода, маяки, звуки событий (`kind:"event"`), всё неназванное |
| VOICE/NPC | `npcVol` | голоса людей: приветствия, торг, стража (`npcCue`) |
| TTS | `voiceVol`, `rate` | речь игры; отдельный синтезатор, не аудиошина |
| COMBAT | `combatVol` | замах, удар, рана, голос твари в бою |
| UI | `uiVol` | сигналы меню |
| MAGIC | `magicVol` | семь слоёв заклинания |

Вид источника переводится в канал таблицей `Bank.CHAN`; неизвестный вид
честно считается миром, а не тишиной. Проверка: набор 98, раздел 1; набор 57.

## 2. Система вариативности (Audio Variation System)

`Vary` хранит хвост последних сыгранных вариантов каждой роли и выбирает
следующий мимо них: роль с двумя записями чередует их, роль с восемью почти
никогда не повторяется. `Bank.play(role,{pitchVar:0.06})` добавляет разброс
скорости — даже единственная запись звучит каждый раз чуть иначе.
`fixed:true` с семенем оставляет прежний детерминированный выбор — для того,
что должно звучать одинаково на одном и том же месте (петли фона).
Проверка: набор 98, раздел 2.

## 3. Приоритеты голосов

`Voices` ограничивает число одновременно звучащих коротких записей
(`Voices.MAX`, четырнадцать). При переполнении уступает не случайный и не
самый старый, а тот, у кого меньше счёт: приоритет (high/mid/low), потом
дальность, потом возраст. По умолчанию музыка, меню и бой — высокий приоритет,
фон — низкий, прочее — средний; явный `prio` в вызове перебивает канал.
Проверка: набор 98, раздел 3.

## 4. Объёмный звук и преграды

Каждый источник ставится в точку сцены (HRTF), а место даёт отзвук
(свёртка с откликом, вычисленным из размеров уровня). Сверх прежнего —
преграды: `Spatial.occlusion(dx,dy,dz)` считает стены по прямой на плане
уровня (стена — 1, дверь и ворота — 0,4, чужой ярус — 1,5), снаружи —
постройки, горы и лес. За преградой звук проходит через фильтр верха
(9000 Гц / (1 + 2,2·преграда)), тише на 28 % за единицу и с большей долей
отзвука. Кузница за стеной слышна иначе, чем кузница рядом; тварь этажом
выше — глуше, чем на этом. Проверка: набор 98, раздел 4; набор 39.

## 5. Звук — событию

Музыки событий больше нет: фанфары, джинглы, оркестровые ноты, хоры и
мелодичные сигналы меню удалены вместе с папками `inst`, `orch`, `mood`,
`folk`, `relic`, `spell`, `arms`, `events`, `gods` и `magic`. Вехи отмечают
живые записи из свободных фэнтезийных игр — монеты, сундук, стекло, голоса
толпы и стражи, колокола, рог победы, гром и ветер. Звук события идёт видом
`kind:"event"` каналом мира (`Bank.CHAN.event="world"`), поэтому выключенная
музыка не глушит весть о награде.

`EVENT_THEME` — первый вариант каждого из 34 событий (у каждого своя первая
роль), `EVENT_POOL` — пулы вариантов по ярусам значимости; `EventPick.choose`
помнит последние три варианта на событие (память переживает перезапуск) и не
повторяет их подряд. `eventTheme(id)` играет роль и её «следом», `eventCue`
— короткую череду записей для войны, званий и репутации.

| Событие | Ярусы | Чем звучит | Откуда зовётся |
|---|---|---|---|
| `levelup` | normal (героический, целительный, магический, спокойный, храмовый), rare, epic | вспышка исцеления, приветствие толпы, клич | `checkLevelUp` |
| `quest_take` | normal, important, chain, story, secret, epic | шаг по гравию, страница, обращение старшего | взятие дела у жителя |
| `quest_done` | normal, success, hard, chain, chapter, unexpected | касса, согласный гул, клич | сдача дела |
| `story_stage` | — | гром, храм, большой колокол | новая ступень цепочки |
| `chain_done` | — | клич толпы, большой колокол, приветствие | цепочка пройдена целиком |
| `achieve` | normal, rare, epic, hidden, story | сундук, находка, петля и замок тайника | мастерства, ветви силы, ловушка |
| `reward` | common (звук вещи), valuable, rare, unique, legendary, artifact, story | монеты, самоцвет, гул артефакта, звон стекла | добыча по рангу вещи |
| `boss_down`, `reward_title` | — | рог победы и клич; приветствие и оклик стражи | владыка пал, титул |
| `victory_great` | — | поступь строя и победный клич; рядовая победа — только звук тела | тварь на три уровня выше |
| `festival` | — | церковные колокола Stendhal и приветствие толпы | праздник бога |
| `region_new`, `place_new` | по роду места | птицы и копыта; ворота и стекло | первая держава, первое место |

Война, звания и репутация звучат `eventCue`: объявление войны — тревога
набега, поступь строя и клич; замирение — рог победы, колокол и толпа; новое
звание — весть о герое, приветствие и обращение старшего; понижение — весть
о павшем герое и колокол; порог отношения народа — свет или тень чар.
Двенадцать богов — двенадцать записей Stendhal (`GOD_SOUND`), граница державы
— оклик стражи и одно из двенадцати дел земли (`EMPIRE_VOICE`).

Проверка: набор 99; наборы 56, 18, 168.

### 5а. Музыка мест из фэнтезийных игр

Фон места — единственная музыка, которая играет подолгу, и она подобрана
заново: каждому ключу `MUSIC_TRACK` (82 ключа) своя роль `mus_*`, и ни одна
запись не звучит в двух местах. Ключи: заглавие и пролог; девять местностей
и небо; бой, бой с сильным, город, чары, затишье, смута; 26 видов построек
(`pl_*`, включая крепость, походный лагерь, схрон, гнездовье, кумирню на
вершине и девять тёмных построек); семь тёмных земель; пять поясов глубины
и дно (`amb_abyss` — у дна музыки нет); двенадцать орденов (`tower_*`);
четырнадцать родов подземелий (`dk_*`).

- `underTrack(d)` — тема под землёй: на ярусах 1–10 род подземелья
  (`dk_<род>`), глубже — пояс глубины `depthTrack(d)`.
- `depthTrack(d)` за Гранью идёт по кругам `DARK_CIRCLE_TRACK`: круг,
  названный по тёмной земле, звучит её темой.
- `Bank.loop` у роли из нескольких записей играет их по кругу одна за другой
  (город, равнина, дорога, лес, бой, бой с сильным, смута, таверна, замок,
  торговый двор, руины); множитель `g` роли выравнивает громкость прежних
  громких записей.

Источники — одиннадцать свободных фэнтезийных игр: Stendhal, Solarus Free
Resource Pack, Valyria Tear, Wyrmsun, Ancient Beast, MegaGlest, OpenDungeons,
Flare, OpenClonk, Evol Online, The Mana World. Под GPL не взято ничего; всё
новое — OGG Vorbis 320 кбит/с, громкость −22 LUFS, темы длиннее 2:15
укорочены до 2:00 с затуханием. Пофайловые таблицы — в `CREDITS.md` папок
`stendhal`, `solarus`, `valyria`, `wyrmsun`, `evol`, `tmw`, `ab`, `mg`.
Проверка: наборы 36, 55, 56.

## 6. Шаг как сведение

Восемнадцать поверхностей (`SURF_ROLE`, `SURF_NAME`): трава, высокая трава,
грязь, мокрая земля, песок, камень, гравий, снег, лёд, дерево, доски, мрамор
(каменный пол), металл, мостки, ступени, болото, мелкая и глубокая вода — и
сверх них прежние: листва, солома, кости, кристалл, пепел, щебень.
`stepProfile` складывает обстоятельства: бег быстрее и громче, железный
доспех ниже и с лязгом, кожа скрипит, ткань шуршит, дождь даёт хлюп, глубина —
эхо, тяжёлая сума и изменённая тяжесть — ниже, тишина аномалии — вполголоса.
Вариант записи берёт `Vary`, скорость плавает, нога чередуется.
Проверка: набор 100, разделы 5–7; наборы 34, 44, 60.

**Шаг по месту и доспеху (набор 186).** Внутри постройки пол один на всё
место (`PLACE_FLOOR`: доски трактира, мрамор храма, металл кузни, солома
рынка…), под землёй — одна порода на ярус (`indoorSurface`); у каждой
поверхности своя роль (`SURF_ROLE`). Тон места (`placeStepTone`) чуть сдвигает
высоту: одна поверхность в двух местах звучит по-разному, в одном — всегда
одинаково. Доспех определяется по материалу вещи (`equipMaterial`), а не по
роду: железо — `hero_step_metal` (звенья кольчуги), кованые сапоги —
`gear_plate`, кожа — `hero_step_leather`, без доспеха — `hero_step_cloth`,
оружие на поясе через шаг — `gear_blade`; записи — `sounds/gear`, FLAC.
Стража (`Actors.step`): двое ближайших идущих в пределах `ACTOR_HEAR` = 8
клеток ступают своим шагом со своей стороны, с лязгом железа или, в местах из
`ACTOR_LEATHER` (деревня, трактир, торг…), с поскрипом кожи; дозор позвякивает
цепью и оружием.

### 6а. Голос жителя: сжатие и подъём (набор 175)

Речь жителей (`sounds/voice`, −18 LUFS) шла в пространство как любой звук
канала `npc`: `speechDuck("npc")` убавлял её под речь игры, шина эффектов
`AE.fx` — ползунком эффектов, общая громкость входила дважды (в `Bank.vol` и
в `AE.master`), отзвук помещения — в полную долю. Теперь `Spatial.at(…,
{voice:true})` и плоский путь `Folk.плоско` ведут речь через
`voiceChain`: `DynamicsCompressor` (порог −36 дБ, 8:1, колено 6, атака 1 мс,
отпуск 200 мс) и подъём ×2,4; громкость — `voiceLevel`, где приглушение,
шина эффектов и второй множитель общей громкости делятся обратно, потолок
`VOICE_MIX.cap` = 2,4. Отзвук — `VOICE_MIX.wet` = 0,4 от обычного, преграда
не больше 1,5 стены, спад с расстоянием 0,75 вместо 1,1. Замер на выходе
(анализатор после `AE.master`): прежде −22,6 дБ RMS, теперь −14,9, под речью
игры −25 → −16; пик −1…−2 дБ.

У `talkInside` шум места — 0,3 и 0,25 вместо 0,55 и 0,45, тележка подземного
торга один раз, маяк лавки (`trader_deep` — та же касса) снят; маяк жителя
в карточке — `beacon(id,pan,dist,k)` с `k` = 0,4, пока житель говорит сам.
Оклик, клич и зазыв прохожих (`ACTOR_VOICE_ROLE`) — тем же путём, ×0,7.

### 6б. Словами — только по-русски (набор 177)

Слова в игре — это живые русские голоса жителей (`sounds/voice`) и голос,
которым игра читает текст (синтезатор устройства, встроенный Piper «Денис»
или ElevenLabs). Всё остальное, что издают люди и твари, — без слов.

**Как проверено.** Весь банк — 1473 записи — прослушан распознавателями
речи: Whisper base (определение языка и расшифровка), русский GigaAM v2
(CTC, без языковой модели, поэтому не выдумывает слов) и, для спорных,
Whisper small. Чужой речью считалась запись, где несколько моделей
согласно слышат одни и те же слова; одиночные «Yes», «What?» и «Thank you»
на рыке и музыке — известные выдумки Whisper, их проверяли повторно.
193 записи `sounds/voice` распознаны как русские (три помечены Whisper как
китайские, но GigaAM расшифровал в них русскую фразу). С версии 3.7 все 407
записей — новое поколение (раздел 6г), и каждая проверена GigaAM по своей
строке.

**Что убрано — 71 запись.** Роли остались, звучат голосами без слов:

| Роль | Было | Стало |
|---|---|---|
| `throng_*` (12) | реплики отрядов 0 A.D. на латыни, греческом, персидском | смешок, оклик, «м-гм», покашливание, клич, вскрик (Stendhal, OpenClonk, MegaGlest); `throng_march` — гул толпы OpenClonk; «за работу» и «иду» — каменотёс и шаги |
| `trade_m`, `trade_f` (10) | английские зазывы Flare | хохот торговца, смех торговки (Stendhal, OpenClonk) |
| `od_keeper_word`, `od_keeper_groan` (28) | английские вести владыки OpenDungeons | смех (Stendhal), рык и стон (MegaGlest) |
| `work_done` (3) | «I did it!», «Finished» рабочего MegaGlest | роль снята |
| `portal` (2) | «It's finished», «It's over» послушника MegaGlest | удары силы MegaGlest |
| `build_magic` (3) | заклинания послушника | удары силы MegaGlest |
| `minaret` (5) | зов с минарета | роль снята (её не звала ни одна строка) |
| `foe_undead_cast` (1) | «brains» зомби Flare | стон нежити (Stendhal) |
| `ad_temple` (2) | хор храма 0 A.D. со словами | голуби под сводами (Stendhal) |
| `cr_ghost_*`, `cr_behemoth_*` (5) | «Yes», «What do you want?» призрачного доспеха и громады MegaGlest | стон и шипение кошмара, рык тролля, рёв льва (Stendhal), удар доспеха (MegaGlest) |

Гомон трактиров и рынков (`stk_inn`, `hall_market`, `ad_trade`,
`st_tavern_crowd`) оставлен: слова в нём не разобрать ни одной моделью.
Набор 177 стережёт происхождение: ни одна строка титров не называет
источником запись со словами.

## 7. Модели предметов

- **Двери** `DOOR_SOUND`: восемь родов (деревянная, тяжёлая, железная,
  ворота, решётка, тайная, старая скрипучая, магическая) × семь состояний
  (открыть, закрыть, запереть, отпереть, попытка открыть запертую, взлом,
  разрушение). Род решает место (`doorKindHere`): оплот — железо, храм —
  тяжёлая, башня — чары, руины — скрип, глубина — скрип, тяжёлая, решётка.
- **Сундуки** `CHEST_SOUND`: открыть, закрыть, замок, ключ, взлом, редкий,
  магический, ловушка, артефактный; ярус по месту (`chestTier`).
- **Книги** `BOOK_SOUND`: перелистать, раскрыть, закрыть, магически раскрыть
  (гримуар, скрижаль, апокриф), читать, применить, сгореть, разрушить.
- **Алтари** `ALTAR_SOUND`: касание, активация, благословение, приношение,
  проклятие, обряд, завершение обряда.

Проверка: набор 100, разделы 1–4; набор 96.

## 8. Оружие

`WEAPON_SOUND`: меч, топор, кинжал, булава, посох, лук, копьё; у каждого
вынуть, убрать, взмах, попадание, блок, парирование, удар по броне, по
плоти, по кости, промах, падение, столкновение; у лука и копья — спуск,
полёт, дерево, камень. Материал твари (`foeMaterial`) решает, чем отвечает
удар: кость у скелета, броня у голема и стража, плоть у прочих. Слабый удар
твари принимает блок или парирование, тяжёлый проходит с тяжестью.
Проверка: набор 101, разделы 1–2.

## 9. Язык школ чар

`SCHOOL_AUDIO`: двадцать восемь школ (двадцать шесть из состава чар, лёд и
молния сверх них), у каждой семь слоёв — подготовка, заряд, произнесение,
движение силы, удар, итог, затухание — с задержками 0, 260, 560, 820, 1080,
1340 и 1750 мс. Всё записями, ни одного инструмента. Проверка: набор 101,
раздел 3; набор 96.

## 10. Профили тварей

`MONSTER_PROFILE`: двенадцать родов (зверь, ползучее, люди, кость, дух,
мертвец, громада, механизм, дракон, птица, глубина, бес) × пятнадцать реплик
(дыхание, движение, шаг, голос, клич, атака, ранение, тяжёлая рана, гибель,
злоба, обнаружение, потеря, испуг, торжество, особенное). Род читается по
имени (`foeFamily`). В бою: обнаружение и клич при встрече, атака при ударе,
ранение, рана или испуг по остатку здоровья, потеря при побеге, гибель при
победе, торжество при поражении; пульс боя — дыхание, движение или голос.
Проверка: набор 101, раздел 4.

## 11. Окружение

`AMBIENCE_SET`: шестнадцать наборов — лес, горы, топь, пещера, подземелье,
город, село, таверна, кузница, храм, дом, замок, равнина, побережье, пустыня,
тёмные земли; у набора взвешенный список голосов и свои добавки на дождь и
ночь. `Ambience.tick` (в такте живой картины) раз в 3–14 секунд ставит голос
из случайной точки в двух–семи клетках вокруг игрока, каналом фона, низким
приоритетом и без повтора подряд. Проверка: набор 101, раздел 5; набор 85.

**Фоны построек и становищ (набор 185).** У двадцати одной постройки
(`PLACE_BED`), восьми становищ (`CAMP_BED`) и входов в руины, пещеры, склепы,
шахты и гнёзда (`STRUCT_BED`) свой долгий фон — тридцать шесть записей в
`sounds/bed`, Ogg Vorbis 320 кбит/с; у трактира и военного лагеря свой днём и
ночью (`PLACE_BED_TIME`). Каждый собран из настоящих записей круговым
сведением: конец переходит в начало без шва. Канал места (`Bank.loop` →
`_seamless`) сверх того играет петлю двумя плеерами внахлёст с перекрёстным
переходом по синусу и косинусу; границы полезной части записи — `LOOP_EDGE`.
Внутри мест и у становищ живой слой окружения молчит — звучит только фон
места.

**Голоса места.** `PlaceVoice` на стыке петли (`Bank.onSeam`) и изредка в
тишине подаёт короткую русскую фразу человека места (`PLACE_VOICE`,
`CAMP_VOICE`, `STRUCT_VOICE`; роли `pv_*`, сто двадцать девять фраз): только
когда молчат речь игры, её очередь, оклик толпы, окна, бой и лестница, не
раньше чем через две с половиной секунды после речи и не чаще раза в
`ПАУЗА` = 22 секунды. Любая речь игры сразу глушит фразу (`PlaceVoice.hush`).

**Подземелье без неба (набор 184).** Под землёй слои поверхности
(`amb0`/`amb1`) и погода глохнут (`Weather.hushed`), фон — голос глубины; на
выходе под небо погода возвращается.

**Заставка (наборы 187, 189).** `Intro` играет `sounds/intro/intro_02.ogg` раз
за запуск: заглавная тема, голос Supertonic 3 (M4, 44,1 кГц) и эффекты — ветер
и гром OpenClonk (CC BY 3.0), вихрь, колокола, стеклянное мерцание и хор Sonic Pi
(CC0); под словами эффекты приглушены на 7 дБ, удары стоят в паузах. Пока она звучит, `Speech._pump` откладывает
всё, кроме речи, вызванной самим игроком; касание, свайп или клавиша
(`Intro.listen`, `userCut`) гасят её за 250 мс, и отложенная речь идёт своим
чередом. Выключатель — `settings.intro`, повтор — кнопка «Прослушать
заставку» (`CMD.intro`).

**Листание (набор 188).** Щелчок листания — `NavClick`: один заранее
загруженный проигрыватель на каждый род щелчка, свайп перематывает его в
начало. В меню и окнах — `ui_next` (`st_ui_clack_one.flac`, один удар), в
настройках — `ui_next_set` (`mg_menu_click_01.flac`); заголовок раздела —
тот же щелчок на 0,85 скорости, край списка — на 0,7, и пункт на краю
называется снова. Речь: листание (`nav`) не проходит через отсев повторов, а
обычная весть мира не обрывает пункт — встаёт за ним в очередь. Синтезатор
браузера снимает отложенный пуск при каждом `cancel()`; встроенный голос
снимает прежние фразы при новой и помнит звук сказанных кусков (до 2,6 млн
отсчётов); приложение ставит фразу с `QUEUE_FLUSH`.

## 13. Качество записей: 320 кбит/с всему новому

**Правило.** Всякая новая запись в `sounds/` — либо без потерь (`flac`,
`wav`), либо сжата не ниже **320 кбит/с**. Набор проверок 152 стережёт
это на каждом прогоне.

**Как устроена проверка.** В `sounds/BITRATE_BASELINE.txt` поимённо
перечислены 1039 записей, пришедших раньше правила (с 3.7 голоса из него
ушли — они пересняты в 320). Всё, чего в этом
списке нет, обязано быть без потерь или в 320. Список закрыт: его длина
проверяется числом, и тихо дописать в него новый файл не выйдет.

**Почему старое не пережимаем.** Пережатие не возвращает того, чего в
файле нет. Запись, сжатая в 96 кбит/с, после пережатия в 320 звучит ровно
так же, но весит втрое больше. Банк весит 497 МБ, из них около половины —
музыка мест в 320 кбит/с; сплошное пережатие остальных подняло бы его к
полутора гигабайтам — без единого нового слышимого призвука, зато с
втрое более долгой загрузкой на телефоне. Это не улучшение, а порча, и
делать её нечестно.

**Где сейчас банк.** 1666 записей: 168 без потерь (`flac`, `wav`) и 1498
сжатая. Из сжатых 190 уже в 320 и выше (среди них вся музыка мест и долгие
фоны земель из папки `fon`), 81 в 256–319, 155 в 192–255, 470 в 128–191,
557 в 64–127 и 45 ниже 64 (короткие удары, отклики тварей и чар — `foe/`,
`cast/`, `arte/`, `mtg/`, которым высокий битрейт почти ничего не даёт).
Пак Neo HQ Mystic Sounds (80 записей в `sea/`, `ambience/`, `places/`,
`dungeon/`), пак Fantasy Sound Library (42 записи в `fantasy/` и `steps/`)
и пак Classic Monster Sounds (42 записи в `monsters/`) убраны: лицензий у
них нет. Голоса восьми существ теперь лежат в `creatures/` — 41 запись
FLAC из MegaGlest, Stendhal и OpenClonk; исходники без потерь у всех, кроме
гудения роя (в Stendhal оно есть только в OGG 98 кбит/с). Ни одна из этих
записей не взята у другого звука игры: одна запись с двумя смыслами путает
слепого игрока. Ноты `orch/`, `spell/`, `folk/` и
прочие инструменты удалены вместе с музыкой событий.

**Что с прежним можно сделать честно.**

1. **Голоса** (`sounds/voice`) синтезируются заново, а не пережимаются: с
   версии 3.7 все 407 записей пересняты голосами Gemini в 320 кбит/с и из
   списка прежних записей ушли (раздел 6г).
2. **Остальное** — только пересборкой из исходников свободных библиотек
   (0 A.D., OpenClonk, SuperTuxKart, Luanti, OpenDungeons, Endless Sky),
   если те отдают записи в лучшем качестве, чем лежит здесь.

### 6в. Женский голос ремёсел и общих слов, 31 фраза заново (набор 189)

Народ женского голоса (`VOICE_RACES[…][2]=="ж"`, таких 101 из 280) окликал
женской записью, а слово ремесла у 39 ремёсел из 46 и все семь общих слов
получал мужскими — поднятыми темпом. Теперь у каждого ремесла есть
`prof_*_f.mp3`, у каждого общего слова — `say_*_f.mp3`: `Folk.ремесло` и
`Folk.общее` (через `Folk.общееФайл`) берут женскую запись, если народ
женского голоса. Запись — Supertonic 3, голос F3, 44,1 кГц, MP3 320 кбит/с,
−18 LUFS; каждая реплика озвучена шестью дублями, взят лучший по GigaAM и
DNSMOS. Строки женского рода поправлены («Иду откуда шла», «Я ушла сюда…»,
«отреклась сама», «не трогаю первой»).

Тридцать одна фраза народов, где словарь встроенного голоса теперь держит
мягкость («кровь», «соль», «только», «конь»), переозвучена тем же голосом
Piper под новыми именами файлов (`race_*_2.mp3`): записи кэшируются как
неизменные, поэтому новое содержимое — новое имя. Доля ошибочных слов по
GigaAM у этих строк упала с 14 до 8 %.

**Ударения.** Каждое многосложное слово озвученных строк сверено с
нейросетью silero-stress (MIT, пакет `silero-stress` 1.5, различает
омографы по смыслу фразы) и вычитано вручную; расхождений со словарём
`tts/ru-lex.tsv` нашлось 140 в 113 строках. Однозначные слова исправлены в
словаре (11 записей заменено, около 90 добавлено), омографы, зависящие от
смысла, подставлены в фонемы своей строки. Строки Piper переозвучены под
новыми именами (`race_*_2.mp3`, `pv_*_2.mp3`), Supertonic получает текст со
знаком U+0301 после ударного гласного. Сам silero-stress в игру не входит —
это инструмент сборки.

### 6г. Все четыреста семь реплик — голосами Gemini (набор 190)

Вся живая речь — 280 окликов народов, 46 мужских и 46 женских слов ремёсел,
7 и 7 общих слов, 21 ход героя — с версии 3.7 произнесена нейроголосами
Gemini (Google, `gemini-3.8-flash-tts`, Interactions API). Тексты прежние.

**Голоса.** Тринадцать готовых голосов; регистр народа задан самим голосом,
без сдвига высоты (сдвиг портил звук): Algenib — грубые низкие народы, Charon
— древние и огромные, Orus — пониже среднего, Enceladus (с придыханием) —
таинственные, Achird — люди и близкие, Puck — малые и бойкие; у народов
женского голоса Gacrux, Sulafat, Achernar и Leda. Слова ремёсел и общие —
Iapetus и Erinome, голос героя — Algieba. Раздача — по регистру народа
(число в `VOICE_RACES`) и по его имени.

**Пакеты.** Запрос несёт два голоса (`speech_config.speakers`) и до 74
реплик; у каждой реплики своя короткая интонация (`speech_metadata.style`):
«rough and gruff», «soft and melodic», «whispering» у Шепчущего, «in pain»
у слова боли. Метка паузы `<long pause>` однажды прозвучала словом «пауза»
вслух — от неё отказались: реплики и так разделены сменой очереди. Одиннадцать
запросов вместе с переозвучками.

**Разрезка и проверка.** Запись пакета дробится по паузам мельче, чем нужно,
куски склеиваются в реплики динамическим программированием по тексту,
распознанному GigaAM; одиночные вдохи по краям отброшены. Каждая реплика —
не больше 12 % ошибочных букв и не медленнее восьми знаков в секунду: первые
дубли трёх голосов шли вдвое медленнее (интонации «slow», «measured»,
«mysterious») и переозвучены с «natural pace». Итог: 369 реплик буква в
букву, 406 — в пределах порога; «Конь чует» распознаватель сливает в
«кончуй».

**Громкость.** Динамический `loudnorm` на двух-трёх секундах промахивался на
±2 дБ, поэтому громкость ставится замером EBU R128 и линейным усилением до
−18 LUFS, пики держит ограничитель с упреждением на −3 дБ (запас на выброс
кодера MP3): у каждой реплики от −18,7 до −17,3 LUFS, пики не выше −1,2 дБ.
Записи Gemini ровнее прежних по громкости (разброс внутри фразы почти
нулевой), поэтому без сжатия они звучат на пару децибел громче прежних, а
по пути голоса (раздел 6а) — так же, около −15 дБ; набор 175 меряет это
средним двух замеров после прогрева записи.

**Имена.** Записи кэшируются как неизменные, поэтому поколение приписано к
имени: `VOICE_GEN="_g"` (`race_lyudi_g.mp3`, `prof_kuznec_f_g.mp3`,
`say_torg_g.mp3`, `hero_torg_g.mp3`); прежние файлы и строки списка
битрейтов убраны. Опись — `sounds/voice/CREDITS.md`, сценарий сборки —
`tools/voice/gemini.py`.

### 6д. Игра говорит голосом Gemini (набор 191)

Просьба игрока (3.9): встроенный голос неразборчив и медленно отвечает, а
нужен голос Gemini с быстрым откликом. Нейросеть Gemini живёт только на
серверах Google, и по сети каждая фраза ждала бы ответа секунды, поэтому
голос сделан записями. Голос Gemini **Iapetus** заранее прочёл 2987
постоянных фраз игры (сто две минуты речи): пункты меню, настроек и
инвентаря, имена окон, частые сообщения. Фразы собрала сама игра — прогон
всех наборов проверок записал всё, что отдавалось речи, а обход окон — все
пункты. Голос выбран из четырёх (Kore, Iapetus, Erinome, Achird) по
распознаванию и по оценке модели Gemini в роли эксперта по доступности.

**Как говорит.** Адаптер `gemini` (`Speech._geminiAdapter`) делит текст на
предложения: записанное звучит записью сразу — через одну-две миллисекунды
после команды, без синтеза и без сети; незаписанное (числа, имена,
меняющийся текст) договаривает синтезатор устройства тем же тактом, соседние
незаписанные предложения — одной фразой. Темп — ползунок скорости:
`gvTempo` переводит его в скорость записи вдвое мягче, чем у синтезатора
(скорость пять — запись в 2,8 раза быстрее), высота голоса сохраняется
(`preservesPitch`). Новая фраза обрывает недосказанную запись.

**Записи.** FLAC без потерь, моно, 24 кГц (родная частота Gemini), −18 LUFS,
пик не выше −1 дБ, тишина по краям срезана; 176 МБ в папке
`sounds/gvoice`. Опись `bank_1.js` грузится скриптом (политика безопасности
страницы закрывает fetch): нормализованная фраза → файл. Пакеты по 150 фраз,
два «говорящих» с одним голосом по очереди — для паузы между фразами;
разрезка по распознанному тексту (GigaAM), не больше 15 % ошибочных букв.

**Умолчание.** С 3.9 голос Gemini — первый пункт выбора синтезатора и
умолчание; прежний выбор «синтезатор устройства» и «встроенный голос» один
раз меняется на Gemini (отметка `gvOn`), голос ElevenLabs не трогается. Под
автоматикой (`navigator.webdriver`) умолчание — синтезатор устройства,
пока не попросят `window.__gvAuto`: проверки речи слушают синтезатор.

### 6е. Заставка 3.9: торжественный живой голос Gemini, торжественная тема, яркие эффекты

`sounds/intro/intro_10.ogg`, 61 с. Голос — Gemini **Sadaltager** (модель
`gemini-3.8-flash-tts`, указания к строкам: торжественно и величаво, с
пафосом, финал — зов, переходящий в торжество). Игроку уходили и спокойные,
«живые» варианты (Sadaltager сдержаннее, Charon, Iapetus), но, переслушав всё,
он выбрал первую запись 3.9 и попросил: эффекты без эха, яркие и
разнообразные, но не поверх голоса, голос громче. Эффекты стоят в паузах:
между строками и в паузах на запятых, расширенных на 0,5–1,2 с (после «тьмы»,
«земли», «станешь», «героем», «легендой», «Прислушайся»); под словами они
тише на 6 дБ; после «изгнанником» пауза вставлена заново. На «героем» —
клинки, клич воина и рог, на «изгнанником» — стрела, захлопнутые ворота и
ветер, на «легендой» — сияние чар, звон победы и колокол. Тема «Road to
Victory» звучит с первой секунды, на 13 % медленнее без смены высоты, и не
приглушается под словами: голос в среднем на 2,5 дБ громче музыки и на
1,5 дБ громче эффектов; все эффекты выровнены к одной громкости, на 1 дБ
громче музыки. Под словами у музыки чуть убраны частоты разборчивости
(около 3 кГц), громкость её не меняется. Стерео широкое: эффекты
пролетают по всей панораме, удары, колокола, гром и ворота объёмные,
музыка расширена. После «древние земли» и «забытые боги» — отдельные паузы
с яркими эффектами (дрожь земли и волчий вой; гром богов с хором и
колоколом), перед «Кем ты станешь» рассказчик листает книгу. Из пяти вариантов с разной музыкой игрок выбрал этот.
Тема «Road to Victory» (Moonthief, CC BY-SA 4.0, из Ancient Beast) с
кульминацией сразу после последней строки. Эффекты сухие, без отзвука (он
есть только у голоса): магический переход, глубокий удар, крик ястреба над
ветром, вспышка и искры чар, волчий вой, дальний водопад, сияние чар, ворон,
рёв из глубины; на конце «бездонные подземелья» — стоны двух призраков, стая
летучих мышей, лязг цепей, шёпот нежити, содрогание земли и рык демона;
драконье пламя, каменные ворота, призыв чар, клинок, конь, свист стрелы,
огненный шквал, гром чар, порыв ветра, портал, мерцание чар, а в кульминации —
мощный удар, колокол, гром, рёв змея, дальний рёв дракона, хор и ястреб в
небе. Все строки поверх музыки распознаются GigaAM без ошибок. Опись — `sounds/intro/CREDITS.md`.

### 6ж. Подземелье слышно сквозь музыку (набор 191)

Жалоба игрока: фон подземелья с музыкой не слышен. Петли глубины записаны
очень по-разному: капель первого яруса −29,5 LUFS, бездна пятого −29,2,
сквозняк −17,9, а музыка около −16. Петли заменены копиями, выровненными по
громкости без потерь (FLAC, суффикс `_n`): −18…−22 LUFS. Под землёй фон
места идёт с долей 0,95 вместо 0,55, живой слой — 0,6 вместо 0,32, а музыка
звучит на 0,6 своей громкости (`depthMusicDuck`, −4,4 дБ).

## 12. Источники и лицензии

Все записи — из свободных проектов с совместимыми лицензиями, пофайлово
описаны в `sounds/<папка>/CREDITS.md`, рядом лежит текст лицензии:
0 A.D. и Flare (CC BY-SA 3.0), OpenClonk (CC BY 3.0), Luanti (CC BY-SA 3.0),
Unknown Horizons (CC BY-SA 3.0), Lugaru (CC BY-SA 3.0), MegaGlest
(CC BY-SA 3.0), OpenDungeons (CC BY-SA 3.0 / CC BY 3.0 / CC0), SuperTuxKart
(пофайлово CC BY-SA / CC BY / CC0 / общественное достояние), Endless Sky,
голоса существ в `creatures` (MegaGlest, Stendhal, OpenClonk —
пофайловая опись в `sounds/creatures/CREDITS.md`; пак Classic Monster
Sounds без лицензии убран), игровые звуки в `gamefx`
(Stendhal, MegaGlest, OpenClonk, 0 A.D., Wyrmsun, Valyria Tear — опись в
`sounds/gamefx/CREDITS.md`, шаги — в `sounds/steps/CREDITS.md`; пак
Fantasy Sound Library без лицензии убран), Stendhal
(`stendhalfx`: CC0, CC BY 3.0, CC BY 4.0), фоны и голоса мест в `fon`
(Stendhal, MegaGlest, 0 A.D., Valyria Tear — пофайловая опись в
`sounds/fon/CREDITS.md`), а также записи под CC0. Музыка
мест — из одиннадцати свободных фэнтезийных игр (см. §5а).
Записи под GPL не брались. Ноты звукового шрифта FluidR3_GM и сэмплы
tonejs-instruments из игры удалены вместе с музыкой событий. Шестая поставка (двери, замки,
решётки, телеги, смех, рык, камнепад, лёд, всплески, капли, землетрясение,
топор, летучие мыши, гнус, двор, горн) — OpenClonk, Luanti и Unknown
Horizons; WAV перекодированы в OGG без иных изменений. Набор 85 следит, что
титры называют каждый файл, набор 36 — что каждый файл на диске нужен.

## 13. Критерии готовности и этапы

1. Аудит — сделан: синтез удалён из шагов, чар, оружия, голосов тварей;
   музыка снята с рядовых действий (обычная победа, добыча, сдача).
2. Архитектура — каналы, вариативность, приоритеты, преграды (набор 98).
3. Звук событий — пулы живых записей по ярусам без повторов (набор 99);
   музыка — только темы мест (§5а).
4. Базовые звуки — шаги и модели предметов (набор 100).
5. Окружение — шестнадцать наборов с тактом (набор 101).
6–7. Бой, чары, твари — модели оружия, семь слоёв школ, профили (набор 101).
8. 3D — преграды, перекрытия, отзвук (наборы 98, 39).
9. Приоритеты — `Voices` (набор 98).
10. Лицензии и документация — этот файл, CREDITS, README.
11. Тестирование — наборы 98–101 и весь прежний прогон.
12. Тонкая настройка — громкости и задержки в таблицах; менять их можно, не
    трогая код: все связки объявлены данными.
